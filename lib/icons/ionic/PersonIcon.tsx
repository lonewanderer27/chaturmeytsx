import { Icon } from "@ui-kitten/components";

export const PersonIcon = (props: any) => {
  return (
    <Icon
      name="person"
      size={props.size}
      color={props.color}
      style={props.style}
    />
  );
}